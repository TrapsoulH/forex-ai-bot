# Tell Lombok to copy @Qualifier to generated constructor parameters
# Required for Spring Framework 6.x + @RequiredArgsConstructor
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
